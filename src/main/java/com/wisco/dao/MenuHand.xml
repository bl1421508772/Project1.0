<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisco.dao.MenuHandDao">
	<insert id="saveMenu" parameterType="com.wisco.entity.Tbmenu" resultType="int"> 
		INSERT INTO Tbmenu SET text=#{text} , state=#{state} , parentId=#{parentId} , iconCls=#{iconCls} , url=#{url}  
	</insert>
</mapper>